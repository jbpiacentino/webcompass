<?php

use Illuminate\Database\Seeder;
use Illuminate\Database\EloquentModel;
use Carbon\Carbon;
use App\User;


class CardsTableSeeder extends Seeder {

	public function run() {

	  	// User::all()->each(function ($user) {
	  	// 	$user->cards()->saveMany(factory(App\Card::class, 7)->make());
	  	// });
	}
}